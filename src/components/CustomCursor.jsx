import React from 'react';

/**
 * Custom Cursor disabled for minimalistic theme.
 * Returns null to allow standard native cursor behavior.
 */
const CustomCursor = () => {
  return null;
};

export default CustomCursor;
